<div class="container show">
	<h1><%= song.name %></h1>
	<h1>by <%= link_to song.artist.name, artist_path(song.artist_id) %></h1>
	<hr>
	<h2>Rating: 
		<% song.rating.times do %>
		<span class="glyphicon glyphicon-star"></span>
		<% end %>
		(<%= song.rating %>/10)
	</h2>
</div>